import { Category } from "@/src/types/category";
import { StyleSheet, View } from "react-native";

const CategoryItem = ({ id, name, image, description }: Category) => {
  return (
    <View>
      <View></View>
    </View>
  );
};

const styles = StyleSheet.create({});

export default CategoryItem;
