import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
     headerContainer: {
          paddingTop: 32,
          paddingHorizontal: 8,
          paddingVertical: 4,
            flexDirection: "row",
            justifyContent: "space-between",
            alignItems: "center",
     },
     rowCenter:{
            flexDirection: "row",
            alignItems: "center",
            gap: 8
     }
});
