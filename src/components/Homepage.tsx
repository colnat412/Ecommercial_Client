
import { ScrollView, Text, View } from "react-native";
import { HomepageHeader } from "./navigation";


export const HomePage = () => {
    return (
        <ScrollView>
            <HomepageHeader />
        </ScrollView>
    );
}
